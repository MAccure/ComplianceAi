<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ComplianceAI - Sales Deck</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <style>
        body {
            font-family: 'Inter', sans-serif;
            background-color: #0a101f;
            color: #e2e8f0;
            overflow: hidden; /* Prevent scrollbars during transitions */
        }
        #particle-canvas {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1; /* Place it behind all other content */
        }
        #presentation-container {
            position: relative;
            width: 100%;
            height: 100vh;
        }
        .slide {
            display: flex;
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            padding: 5rem 4rem;
            box-sizing: border-box;
            opacity: 0;
            transform: scale(0.95);
            transition: opacity 0.6s cubic-bezier(0.4, 0, 0.2, 1), transform 0.6s cubic-bezier(0.4, 0, 0.2, 1);
            visibility: hidden;
        }
        .slide.active {
            opacity: 1;
            transform: scale(1);
            visibility: visible;
            z-index: 10;
        }
        .glass-panel {
            background: rgba(15, 23, 42, 0.7); /* Slightly more opaque */
            backdrop-filter: blur(12px);
            border: 1px solid rgba(56, 189, 248, 0.2);
            border-radius: 1.5rem;
            padding: 2.5rem;
            box-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.37);
        }
        .nav-arrow {
            position: fixed;
            top: 50%;
            transform: translateY(-50%);
            background: rgba(30, 41, 59, 0.7);
            color: #e2e8f0;
            border-radius: 50%;
            width: 3rem;
            height: 3rem;
            display: flex;
            justify-content: center;
            align-items: center;
            cursor: pointer;
            transition: all 0.3s ease;
            z-index: 1000;
            border: 1px solid rgba(56, 189, 248, 0.2);
        }
        .nav-arrow:hover {
            background: rgba(56, 189, 248, 0.8);
            color: #0a192f;
        }
        #prev-btn { left: 1rem; }
        #next-btn { right: 1rem; }
        .progress-bar {
            position: fixed;
            bottom: 0;
            left: 0;
            height: 6px;
            background-color: #38bdf8;
            transition: width 0.3s ease;
            z-index: 1000;
        }
        /* Staggered Animation for slide content */
        .animated-item {
            opacity: 0;
            transform: translateY(20px);
            transition: opacity 0.5s ease-out, transform 0.5s ease-out;
        }
        .slide.active .animated-item {
            opacity: 1;
            transform: translateY(0);
        }
        .slide.active .delay-1 { transition-delay: 0.2s; }
        .slide.active .delay-2 { transition-delay: 0.3s; }
        .slide.active .delay-3 { transition-delay: 0.4s; }
        .slide.active .delay-4 { transition-delay: 0.5s; }

        .btn-primary {
            background: linear-gradient(90deg, #38bdf8, #3b82f6);
            color: white;
            padding: 0.75rem 1.5rem;
            border-radius: 9999px;
            font-weight: 600;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(59, 130, 246, 0.3);
        }
        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(59, 130, 246, 0.5);
        }
        .slide-header {
            position: absolute;
            top: 2rem;
            left: 4rem;
            display: flex;
            align-items: center;
            gap: 1rem;
        }
        .calculator-input {
            background: rgba(15, 23, 42, 0.8);
            border: 1px solid rgba(56, 189, 248, 0.3);
            color: #e2e8f0;
        }
        .roi-card {
            background: linear-gradient(145deg, rgba(30, 41, 59, 0.8), rgba(15, 23, 42, 0.8));
            border: 1px solid rgba(56, 189, 248, 0.2);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }
        .roi-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 30px rgba(56, 189, 248, 0.2);
        }
        .industry-tag {
            cursor: pointer;
            transition: all 0.3s ease;
            border: 1px solid rgba(56, 189, 248, 0.3);
        }
        .industry-tag:hover, .industry-tag.active {
            background-color: #38bdf8;
            color: #0a192f;
            transform: scale(1.05);
        }
    </style>
</head>
<body>

    <canvas id="particle-canvas"></canvas>

    <!-- Slide Container -->
    <main id="presentation-container">

        <!-- Slide 1: Title -->
        <section class="slide active" id="slide1">
            <div class="glass-panel text-center w-full max-w-4xl">
                <h1 class="text-5xl md:text-6xl font-black text-white tracking-tight animated-item">ComplianceAI</h1>
                <p class="mt-4 text-xl md:text-2xl text-sky-300 font-medium animated-item delay-1">The Future of Your Compliance Practice.</p>
                <p class="mt-8 text-lg text-slate-300 animated-item delay-2">Transforming Compliance from a Cost Center to a Profit Driver.</p>
            </div>
        </section>

        <!-- Slide 2: The Compliance Crisis -->
        <section class="slide" id="slide2">
            <div class="slide-header animated-item">
                <span class="text-slate-400">The Problem</span>
            </div>
            <div class="w-full max-w-6xl grid md:grid-cols-2 gap-12 items-center">
                <div class="animated-item">
                    <h2 class="text-4xl font-bold text-sky-300 mb-4 animated-item delay-1">The Compliance Gauntlet</h2>
                    <p class="text-lg text-slate-300 mb-4 animated-item delay-2">Manual compliance is a bottleneck. It consumes hundreds of billable hours, limits your firm's growth, and exposes your clients to significant risk.</p>
                    <ul class="space-y-3 text-slate-300 animated-item delay-3">
                        <li class="flex items-start gap-3"><i class="fa-solid fa-hourglass-half text-sky-400 mt-1"></i><span><strong>Labor-Intensive:</strong> Hundreds of hours per audit cycle are spent on manual evidence collection and gap analysis.</span></li>
                        <li class="flex items-start gap-3"><i class="fa-solid fa-magnifying-glass-chart text-sky-400 mt-1"></i><span><strong>Error-Prone:</strong> Manual data entry and interpretation lead to inconsistencies and increase the risk of costly oversights.</span></li>
                        <li class="flex items-start gap-3"><i class="fa-solid fa-arrow-trend-down text-sky-400 mt-1"></i><span><strong>Limits Scalability:</strong> Your firm's capacity is tied directly to headcount, creating a barrier to taking on new clients.</span></li>
                    </ul>
                </div>
                <div class="glass-panel p-4 animated-item delay-2">
                    <h3 class="text-2xl font-bold text-center mb-4 text-white">The Staggering Cost of Manual Audits</h3>
                    <canvas id="auditCostChart"></canvas>
                </div>
            </div>
        </section>

        <!-- Slide 3: Introducing ComplianceAI -->
        <section class="slide" id="slide3">
             <div class="slide-header animated-item">
                <span class="text-slate-400">The Solution</span>
            </div>
            <div class="text-center w-full max-w-5xl">
                <h2 class="text-4xl md:text-5xl font-bold text-white animated-item">Transform Your Practice with <span class="text-sky-300">ComplianceAI</span></h2>
                <p class="mt-4 text-xl text-slate-300 max-w-3xl mx-auto animated-item delay-1">Move from manual drudgery to AI-powered efficiency. Empower your team, delight your clients, and unlock new revenue streams.</p>
                <div class="mt-12 grid md:grid-cols-2 gap-8 items-center animated-item delay-2">
                    <div class="text-left p-6 rounded-2xl bg-slate-800/50">
                        <h3 class="text-2xl font-bold mb-4 text-red-400 flex items-center gap-3"><i class="fa-solid fa-circle-xmark"></i> Before</h3>
                        <ul class="space-y-2 text-slate-300">
                            <li><i class="fa-solid fa-minus text-red-400/50 mr-2"></i>Hours spent deciphering regulations</li>
                            <li><i class="fa-solid fa-minus text-red-400/50 mr-2"></i>Manual evidence-to-control mapping</li>
                            <li><i class="fa-solid fa-minus text-red-400/50 mr-2"></i>Inconsistent, static reports</li>
                            <li><i class="fa-solid fa-minus text-red-400/50 mr-2"></i>Client capacity constraints</li>
                        </ul>
                    </div>
                     <div class="text-left p-6 rounded-2xl bg-slate-800/50 border border-sky-400">
                        <h3 class="text-2xl font-bold mb-4 text-green-400 flex items-center gap-3"><i class="fa-solid fa-circle-check"></i> After with ComplianceAI</h3>
                        <ul class="space-y-2 text-slate-200">
                            <li><i class="fa-solid fa-plus text-green-400/50 mr-2"></i>Instant, AI-generated checklists</li>
                            <li><i class="fa-solid fa-plus text-green-400/50 mr-2"></i>Automated evidence analysis</li>
                            <li><i class="fa-solid fa-plus text-green-400/50 mr-2"></i>Real-time, dynamic dashboards</li>
                            <li><i class="fa-solid fa-plus text-green-400/50 mr-2"></i><strong>3x client capacity increase</strong></li>
                        </ul>
                    </div>
                </div>
            </div>
        </section>

        <!-- Slide 4: How It Works -->
        <section class="slide" id="slide4">
            <div class="slide-header animated-item">
                <span class="text-slate-400">How It Works</span>
            </div>
            <div class="text-center w-full max-w-6xl">
                 <h2 class="text-4xl font-bold text-white mb-12 animated-item">Compliance in 3 Simple Steps</h2>
                 <div class="grid md:grid-cols-3 gap-8">
                    <div class="glass-panel text-center animated-item delay-1">
                        <div class="text-5xl text-sky-400 mb-4"><i class="fa-solid fa-list-check"></i></div>
                        <h3 class="text-2xl font-bold mb-2 text-white">1. Generate Checklist</h3>
                        <p class="text-slate-300">Instantly turn complex regulations like HIPAA, SOX, or GDPR into clear, actionable audit checklists. Our AI does the heavy lifting.</p>
                    </div>
                    <div class="glass-panel text-center animated-item delay-2">
                        <div class="text-5xl text-sky-400 mb-4"><i class="fa-solid fa-file-arrow-up"></i></div>
                        <h3 class="text-2xl font-bold mb-2 text-white">2. Map Evidence</h3>
                        <p class="text-slate-300">Securely upload client documents—policies, procedures, logs. ComplianceAI automatically maps them to the relevant controls.</p>
                    </div>
                    <div class="glass-panel text-center animated-item delay-3">
                        <div class="text-5xl text-sky-400 mb-4"><i class="fa-solid fa-chart-pie"></i></div>
                        <h3 class="text-2xl font-bold mb-2 text-white">3. Analyze & Report</h3>
                        <p class="text-slate-300">Get instant gap analysis, risk scoring, and remediation advice. Export white-labeled, client-ready reports in minutes.</p>
                    </div>
                 </div>
            </div>
        </section>
        
        <!-- Slide 5: The "Wow Factor" - ROI Calculator -->
        <section class="slide" id="slide5">
            <div class="slide-header animated-item">
                <span class="text-slate-400">The Business Case</span>
            </div>
            <div class="w-full max-w-7xl">
                <div class="text-center mb-12">
                    <h2 class="text-4xl md:text-5xl font-bold text-white animated-item">See Your Firm's Potential ROI</h2>
                    <p class="mt-4 text-xl text-slate-300 max-w-3xl mx-auto animated-item delay-1">Quantify the impact of ComplianceAI on your practice. Adjust the sliders to match your firm's metrics.</p>
                </div>
                <div class="grid md:grid-cols-2 gap-8 items-start">
                    <!-- Calculator Inputs -->
                    <div class="glass-panel p-8 space-y-6 animated-item delay-2">
                        <div>
                            <label for="auditsPerYear" class="block mb-2 font-medium text-slate-300">Audits / Year: <span id="auditsValue" class="font-bold text-sky-300">100</span></label>
                            <input type="range" id="auditsPerYear" min="10" max="500" value="100" step="10" class="w-full h-2 bg-slate-700 rounded-lg appearance-none cursor-pointer">
                        </div>
                        <div>
                            <label for="hoursPerAudit" class="block mb-2 font-medium text-slate-300">Avg. Manual Hours / Audit: <span id="hoursValue" class="font-bold text-sky-300">150</span></label>
                            <input type="range" id="hoursPerAudit" min="20" max="500" value="150" class="w-full h-2 bg-slate-700 rounded-lg appearance-none cursor-pointer">
                        </div>
                        <div>
                            <label for="hourlyRate" class="block mb-2 font-medium text-slate-300">Avg. Blended Hourly Rate: $<span id="rateValue" class="font-bold text-sky-300">250</span></label>
                            <input type="range" id="hourlyRate" min="50" max="1000" value="250" step="10" class="w-full h-2 bg-slate-700 rounded-lg appearance-none cursor-pointer">
                        </div>
                    </div>
                    <!-- ROI Outputs -->
                    <div class="grid grid-cols-1 sm:grid-cols-2 gap-6 animated-item delay-3">
                        <div class="roi-card p-6 rounded-2xl text-center">
                            <p class="text-slate-400 text-sm font-medium">Annual Cost Savings</p>
                            <p id="costSavings" class="text-4xl font-bold text-green-400 mt-2">$2.7M</p>
                        </div>
                        <div class="roi-card p-6 rounded-2xl text-center">
                            <p class="text-slate-400 text-sm font-medium">Time Saved Per Year</p>
                            <p id="timeSaved" class="text-4xl font-bold text-sky-300 mt-2">13,500 <span class="text-2xl">hrs</span></p>
                        </div>
                        <div class="roi-card p-6 rounded-2xl text-center">
                            <p class="text-slate-400 text-sm font-medium">Efficiency Gain</p>
                            <p id="efficiencyGain" class="text-4xl font-bold text-sky-300 mt-2">~90%</p>
                            <p class="text-slate-400 text-xs">(vs. manual process)</p>
                        </div>
                        <div class="roi-card p-6 rounded-2xl text-center">
                            <p class="text-slate-400 text-sm font-medium">Increased Client Capacity</p>
                            <p id="capacityIncrease" class="text-4xl font-bold text-green-400 mt-2">300+</p>
                            <p class="text-slate-400 text-xs">(audits with same team)</p>
                        </div>
                    </div>
                </div>
            </div>
        </section>
        
        <!-- Slide 6: Key Features for Law Firms -->
        <section class="slide" id="slide6">
            <div class="slide-header animated-item">
                <span class="text-slate-400">Platform Features</span>
            </div>
            <div class="w-full max-w-6xl">
                <h2 class="text-4xl font-bold text-center text-white mb-12 animated-item">Built for Your Practice, and Your Clients</h2>
                <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
                    <div class="glass-panel p-6 animated-item delay-1">
                        <div class="text-3xl text-sky-400 mb-3"><i class="fa-solid fa-users"></i></div>
                        <h3 class="text-xl font-bold text-white mb-2">Multi-Tenant Architecture</h3>
                        <p class="text-slate-300">Manage all your clients from a single, secure dashboard. Data is completely segregated, ensuring confidentiality and peace of mind.</p>
                    </div>
                    <div class="glass-panel p-6 animated-item delay-2">
                        <div class="text-3xl text-sky-400 mb-3"><i class="fa-solid fa-file-invoice"></i></div>
                        <h3 class="text-xl font-bold text-white mb-2">White-Labeled Reporting</h3>
                        <p class="text-slate-300">Deliver beautiful, insightful reports and dashboards branded with your firm's logo. Reinforce your value and professionalism.</p>
                    </div>
                    <div class="glass-panel p-6 animated-item delay-3">
                        <div class="text-3xl text-sky-400 mb-3"><i class="fa-solid fa-shield-halved"></i></div>
                        <h3 class="text-xl font-bold text-white mb-2">Enterprise-Grade Security</h3>
                        <p class="text-slate-300">Built on a foundation of security, with role-based access controls to manage permissions for your team and your clients.</p>
                    </div>
                    <div class="glass-panel p-6 lg:col-span-3 animated-item delay-4">
                        <div class="text-3xl text-sky-400 mb-3"><i class="fa-solid fa-globe"></i></div>
                        <h3 class="text-xl font-bold text-white mb-2">Unmatched Framework Support</h3>
                        <p class="text-slate-300 mb-4">From healthcare to finance to cybersecurity, our AI supports a vast library of compliance frameworks out-of-the-box.</p>
                        <div class="flex flex-wrap gap-3 text-sm">
                            <span class="bg-slate-700/50 text-sky-300 py-1 px-3 rounded-full">HIPAA</span>
                            <span class="bg-slate-700/50 text-sky-300 py-1 px-3 rounded-full">SOX</span>
                            <span class="bg-slate-700/50 text-sky-300 py-1 px-3 rounded-full">GDPR</span>
                            <span class="bg-slate-700/50 text-sky-300 py-1 px-3 rounded-full">PCI DSS</span>
                            <span class="bg-slate-700/50 text-sky-300 py-1 px-3 rounded-full">NIST CSF</span>
                            <span class="bg-slate-700/50 text-sky-300 py-1 px-3 rounded-full">ISO 27001</span>
                            <span class="bg-slate-700/50 text-sky-300 py-1 px-3 rounded-full">FSMA</span>
                            <span class="bg-slate-700/50 text-sky-300 py-1 px-3 rounded-full">FFIEC</span>
                             <span class="bg-slate-700/50 text-sky-300 py-1 px-3 rounded-full">and 100+ more...</span>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <!-- Slide 7: Use Cases -->
        <section class="slide" id="slide7">
            <div class="slide-header animated-item">
                <span class="text-slate-400">Use Cases</span>
            </div>
            <div class="w-full max-w-7xl">
                <h2 class="text-4xl font-bold text-center text-white mb-4 animated-item">Expand Your Practice Across Industries</h2>
                <p class="text-lg text-center text-slate-300 mb-8 animated-item delay-1">Click an industry to see how ComplianceAI streamlines audits.</p>
                <div class="flex flex-wrap justify-center gap-3 mb-8 animated-item delay-2">
                    <div class="industry-tag bg-slate-700/50 text-slate-300 py-2 px-4 rounded-full active" data-industry="healthcare">Healthcare</div>
                    <div class="industry-tag bg-slate-700/50 text-slate-300 py-2 px-4 rounded-full" data-industry="finance">Finance</div>
                    <div class="industry-tag bg-slate-700/50 text-slate-300 py-2 px-4 rounded-full" data-industry="cyber">Cybersecurity</div>
                    <div class="industry-tag bg-slate-700/50 text-slate-300 py-2 px-4 rounded-full" data-industry="payments">Payments</div>
                </div>
                <div id="use-case-content" class="glass-panel p-8 min-h-[20rem] animated-item delay-3">
                    <!-- JS will populate this -->
                </div>
            </div>
        </section>

        <!-- Slide 8: Next Steps -->
        <section class="slide" id="slide8">
            <div class="text-center w-full max-w-4xl">
                <h2 class="text-5xl font-bold text-white mb-6 animated-item">Ready to Redefine Compliance?</h2>
                <p class="text-xl text-slate-300 mb-12 animated-item delay-1">Let's discuss how ComplianceAI can be tailored to your firm's unique needs and growth objectives.</p>
                <div class="animated-item delay-2">
                    <a href="https://outlook.office.com/bookwithme/user/d4fcb8e795e54bb1adb523aa6e640372%40accure.ai?anonymous&ismsaljsauthenabled" target="_blank" class="btn-primary text-xl">
                        Schedule a Personalized Demo
                    </a>
                </div>
                 <div class="mt-12 text-slate-400 animated-item delay-3">
                    <p>Contact us at <a href="mailto:info@accure.ai" class="text-sky-400 hover:underline">info@accure.ai</a> or call +1-703-889-8834</p>
                    <p class="text-sm mt-2">Built by Accure | <a href="https://accure.ai" target="_blank" class="hover:underline">accure.ai</a></p>
                </div>
            </div>
        </section>

    </main>

    <!-- Navigation -->
    <div id="prev-btn" class="nav-arrow"><i class="fas fa-arrow-left"></i></div>
    <div id="next-btn" class="nav-arrow"><i class="fas fa-arrow-right"></i></div>
    <div id="progress-bar" class="progress-bar"></div>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const slides = document.querySelectorAll('.slide');
            const prevBtn = document.getElementById('prev-btn');
            const nextBtn = document.getElementById('next-btn');
            const progressBar = document.getElementById('progress-bar');
            let currentSlide = 0;
            const totalSlides = slides.length;

            function updatePresentation() {
                slides.forEach((slide, index) => {
                    slide.classList.toggle('active', index === currentSlide);
                });
                const progress = ((currentSlide + 1) / totalSlides) * 100;
                progressBar.style.width = `${progress}%`;
                prevBtn.style.display = currentSlide === 0 ? 'none' : 'flex';
                nextBtn.style.display = currentSlide === totalSlides - 1 ? 'none' : 'flex';
            }

            function showNextSlide() {
                if (currentSlide < totalSlides - 1) {
                    currentSlide++;
                    updatePresentation();
                }
            }

            function showPrevSlide() {
                if (currentSlide > 0) {
                    currentSlide--;
                    updatePresentation();
                }
            }

            nextBtn.addEventListener('click', showNextSlide);
            prevBtn.addEventListener('click', showPrevSlide);
            
            document.addEventListener('keydown', (e) => {
                if (e.key === 'ArrowRight') showNextSlide();
                if (e.key === 'ArrowLeft') showPrevSlide();
            });
            
            // --- Particle Background ---
            const canvas = document.getElementById('particle-canvas');
            const ctx = canvas.getContext('2d');
            let particles = [];

            function resizeCanvas() {
                canvas.width = window.innerWidth;
                canvas.height = window.innerHeight;
            }
            resizeCanvas();

            class Particle {
                constructor() {
                    this.x = Math.random() * canvas.width;
                    this.y = Math.random() * canvas.height;
                    this.vx = Math.random() * 0.4 - 0.2; // Slower speed
                    this.vy = Math.random() * 0.4 - 0.2;
                    this.radius = Math.random() * 1.5 + 0.5;
                }

                update() {
                    this.x += this.vx;
                    this.y += this.vy;

                    if (this.x < 0 || this.x > canvas.width) this.vx *= -1;
                    if (this.y < 0 || this.y > canvas.height) this.vy *= -1;
                }

                draw() {
                    ctx.beginPath();
                    ctx.arc(this.x, this.y, this.radius, 0, Math.PI * 2);
                    ctx.fillStyle = 'rgba(56, 189, 248, 0.7)'; // Theme color
                    ctx.fill();
                }
            }

            function createParticles() {
                particles = [];
                const particleCount = (canvas.width * canvas.height) / 10000; // Responsive particle count
                for (let i = 0; i < particleCount; i++) {
                    particles.push(new Particle());
                }
            }

            function connectParticles() {
                for (let i = 0; i < particles.length; i++) {
                    for (let j = i; j < particles.length; j++) {
                        const dx = particles[i].x - particles[j].x;
                        const dy = particles[i].y - particles[j].y;
                        const distance = Math.sqrt(dx * dx + dy * dy);

                        if (distance < 120) { // Connection distance
                            ctx.beginPath();
                            ctx.strokeStyle = `rgba(56, 189, 248, ${1 - distance / 120})`; // Fade with distance
                            ctx.lineWidth = 0.3;
                            ctx.moveTo(particles[i].x, particles[i].y);
                            ctx.lineTo(particles[j].x, particles[j].y);
                            ctx.stroke();
                        }
                    }
                }
            }

            function animate() {
                ctx.clearRect(0, 0, canvas.width, canvas.height);
                particles.forEach(p => {
                    p.update();
                    p.draw();
                });
                connectParticles();
                requestAnimationFrame(animate);
            }

            window.addEventListener('resize', () => {
                resizeCanvas();
                createParticles();
            });

            createParticles();
            animate();


            // --- Chart.js for Slide 2 ---
            const auditCostCtx = document.getElementById('auditCostChart');
            if (auditCostCtx) {
                new Chart(auditCostCtx, {
                    type: 'bar',
                    data: {
                        labels: ['Small Firm', 'Medium Firm', 'Large Firm'],
                        datasets: [
                            { label: 'Financial/External', data: [8000, 18000, 38000], backgroundColor: 'rgba(56, 189, 248, 0.7)' },
                            { label: 'SOC 2/IT', data: [18000, 45000, 110000], backgroundColor: 'rgba(239, 68, 68, 0.7)' },
                            { label: 'Compliance/Operational', data: [4000, 9000, 28000], backgroundColor: 'rgba(245, 158, 11, 0.7)' }
                        ]
                    },
                    options: {
                        responsive: true,
                        plugins: { legend: { position: 'top', labels: { color: '#cbd5e1' } } },
                        scales: {
                            y: { beginAtZero: true, ticks: { color: '#94a3b8', callback: value => '$' + value/1000 + 'k' }, grid: { color: 'rgba(255, 255, 255, 0.1)' } },
                            x: { ticks: { color: '#94a3b8' }, grid: { color: 'rgba(255, 255, 255, 0.05)' } }
                        }
                    }
                });
            }

            // --- ROI Calculator Logic for Slide 5 ---
            const sliders = {
                audits: document.getElementById('auditsPerYear'),
                hours: document.getElementById('hoursPerAudit'),
                rate: document.getElementById('hourlyRate')
            };
            const values = {
                audits: document.getElementById('auditsValue'),
                hours: document.getElementById('hoursValue'),
                rate: document.getElementById('rateValue')
            };
            const outputs = {
                cost: document.getElementById('costSavings'),
                time: document.getElementById('timeSaved'),
                efficiency: document.getElementById('efficiencyGain'),
                capacity: document.getElementById('capacityIncrease')
            };

            function formatNumber(num) {
                if (num >= 1000000) return '$' + (num / 1000000).toFixed(1) + 'M';
                if (num >= 1000) return '$' + (num / 1000).toFixed(0) + 'k';
                return '$' + num;
            }

            function calculateROI() {
                const audits = parseInt(sliders.audits.value);
                const hours = parseInt(sliders.hours.value);
                const rate = parseInt(sliders.rate.value);

                values.audits.textContent = audits;
                values.hours.textContent = hours;
                values.rate.textContent = rate;

                const timeSavingsPercentage = 0.90;
                const hoursWithAI = hours * (1 - timeSavingsPercentage);
                const hoursSavedPerAudit = hours - hoursWithAI;
                const totalHoursSaved = audits * hoursSavedPerAudit;
                const totalCostSavings = totalHoursSaved * rate;
                const originalTotalHours = audits * hours;
                const efficiency = originalTotalHours > 0 ? (1 - ((audits * hoursWithAI) / originalTotalHours)) * 100 : 0;
                const newCapacity = Math.floor(originalTotalHours / hoursWithAI);

                outputs.cost.textContent = formatNumber(totalCostSavings);
                outputs.time.innerHTML = totalHoursSaved.toLocaleString() + ' <span class="text-2xl">hrs</span>';
                outputs.efficiency.innerHTML = `~${efficiency.toFixed(0)}%`;
                outputs.capacity.textContent = newCapacity.toLocaleString() + '+';
            }
            Object.values(sliders).forEach(s => s.addEventListener('input', calculateROI));

            // --- Use Case Logic for Slide 7 ---
            const useCaseContent = document.getElementById('use-case-content');
            const industryTags = document.querySelectorAll('.industry-tag');
            
            const useCases = {
                healthcare: {
                    title: 'HIPAA Security and Privacy',
                    icon: 'fa-briefcase-medical',
                    current: 'Manual document review, spreadsheet audits, and reliance on external consultants.',
                    withAI: 'Automated HIPAA checklists, evidence-to-requirement mapping, and real-time gap analysis reports.',
                    roi: 'Reduce initial audit time from <strong>120+ hours to just 10</strong>. Avoid fines of over $50K per violation.'
                },
                finance: {
                    title: 'SOX Financial Reporting',
                    icon: 'fa-landmark',
                    current: 'Manual control testing, tedious spreadsheet tracking, and cumbersome quarterly certifications.',
                    withAI: 'AI-generated SOX checklists and automated evidence matching for internal controls.',
                    roi: 'Cut control testing time from <strong>160 hours to 15</strong>. Save over $60K in annual audit prep costs.'
                },
                cyber: {
                    title: 'NIST CSF & ISO 27001',
                    icon: 'fa-network-wired',
                    current: 'Time-consuming manual risk assessments and paper-based incident response tracking.',
                    withAI: 'Automated NIST/ISO checklist generation and dynamic risk dashboards for a clear security posture.',
                    roi: 'Slash risk review time from <strong>180 hours to 15</strong>. Accelerate certifications and reduce breach risks.'
                },
                payments: {
                    title: 'PCI DSS v4.0',
                    icon: 'fa-credit-card',
                    current: 'Manual review of vulnerability scans and expensive, lengthy annual Report on Compliance (ROC) prep.',
                    withAI: 'Automated PCI DSS checklists, continuous gap analysis, and pre-audit readiness reports.',
                    roi: 'Condense PCI assessment from <strong>140 hours to 12</strong>. Save $30K+ in audit prep and reduce breach fines.'
                }
            };
            
            function updateUseCase(industry) {
                const data = useCases[industry];
                useCaseContent.innerHTML = `
                    <h3 class="text-3xl font-bold text-sky-300 mb-4 flex items-center gap-4"><i class="fa-solid ${data.icon}"></i> ${data.title}</h3>
                    <div class="grid md:grid-cols-3 gap-6 text-slate-300">
                        <div>
                            <h4 class="font-semibold text-white mb-2 border-b border-sky-400/30 pb-1">Current Process</h4>
                            <p>${data.current}</p>
                        </div>
                        <div>
                            <h4 class="font-semibold text-white mb-2 border-b border-sky-400/30 pb-1">With ComplianceAI</h4>
                            <p>${data.withAI}</p>
                        </div>
                        <div>
                            <h4 class="font-semibold text-white mb-2 border-b border-sky-400/30 pb-1">The Impact (ROI)</h4>
                            <p>${data.roi}</p>
                        </div>
                    </div>
                `;
            }
            
            industryTags.forEach(tag => {
                tag.addEventListener('click', () => {
                    industryTags.forEach(t => t.classList.remove('active'));
                    tag.classList.add('active');
                    updateUseCase(tag.getAttribute('data-industry'));
                });
            });

            // Initial calls
            updatePresentation();
            calculateROI();
            updateUseCase('healthcare');
        });
    </script>

</body>
</html>